trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: UseNode@1
  inputs:
    version: '20.x'

- script: npm ci
  displayName: 'Install playwright dependencies'

- script: npx playwright install --with-deps
  displayName: 'Install Playwright Browsers'

- script: SECRET_KEY=skbnawaznusrathfathimaskbnawaznusrathfathimanus npx playwright test
  env:
    USER_NAME: $(USER_NAME)
    PASSWORD: $(PASSWORD)
  displayName: 'Run playwright test'